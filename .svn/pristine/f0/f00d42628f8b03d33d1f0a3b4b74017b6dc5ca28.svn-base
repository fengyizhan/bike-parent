package com.tiamaes.bike.exporter.integrated.photo.persistence;

import java.util.List;

import com.tiamaes.bike.common.bean.integrated.PhotoRecord;

public interface PhotoQueryMapper {
	
	/**
	 * 获取设备拍照记录分页数据
	 * @param photoRecord
	 * @return
	 */
	List<PhotoRecord> getListOfPhotoRecords(PhotoRecord photoRecord);
	
	/**
	 * 获取设备拍照记录总记录数
	 * @param photoRecord
	 * @return
	 */
	int getTotalOfPhotoRecords(PhotoRecord photoRecord);

}
